
run:
    cd backend && node server.js

# Git operations (commented out as example)
# push:
#     @if [ -z "$(msg)" ]; then \
#         echo "Please provide a commit message: make push msg='Your message'"; \
#         exit 1; \
#     fi
#     git add -A
#     git commit -m "$(msg)"
#     git push