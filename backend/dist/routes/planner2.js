"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const express = require('express');
const { getBestClassesPDF, getBestClassesExtension, getClassInfo, getOptimalSchedule } = require('../controllers/planner2');
const router = express.Router();
router.post("/pdf", express.raw({ type: "application/pdf", limit: "10mb" }), getBestClassesPDF);
router.post("/extension", express.json(), getBestClassesExtension);
router.post("/class", getClassInfo);
router.post("/optimalSchedule", getOptimalSchedule);
module.exports = router;
